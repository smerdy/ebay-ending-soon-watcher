link(rel="stylesheet", href="./css/home.css", type="text/css",charset="UTF-8")

each item in ebayItems
  a(href=item.url)
    h3 #{item.title}
  img(src=item.picURL, decoding="async")
  p $#{item.price}
  p #{item.hoursLeft} hours remaining